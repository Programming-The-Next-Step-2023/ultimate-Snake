import setuptools

setuptools.setup(
    name = "Snake_ultimate",
    version = "0.0.1",
    author = "pia",
    author_email = "pia.koch@student.uva.nl",
    python_requires = ">=3.6",
    packages = setuptools.find_packages()
)